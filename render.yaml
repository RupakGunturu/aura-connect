services:
  - type: web_service
    name: aura-connect-backend
    env: node
    region: oregon
    plan: free
    branch: main
    root: backend
    buildCommand: npm install
    startCommand: npm run dev
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
